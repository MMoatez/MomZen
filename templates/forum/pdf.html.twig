<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des Forums</title>
    <style>
        body { font-family: Helvetica, sans-serif; }
        h1 { color: #2c3e50; text-align: center; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid #ddd; padding: 12px; text-align: left; }
        th { background-color: #f8f9fa; }
        img { max-width: 150px; height: auto; display: block; margin: 0 auto; }
        .text-muted { color: #6c757d; }
    </style>
</head>
<body>
    <h1>Liste des discussions du forum</h1>
    
    <table>
        <thead>
            <tr>
                <th>Titre</th>
                <th>Contenu</th>
                <th>Image</th>
                <th>Date de publication</th>
            </tr>
        </thead>
        <tbody>
            {% for forum in forums %}
            <tr>
                <td>{{ forum.titre }}</td>
                <td>{{ forum.contenu }}</td>
                <td>
                    {% if forum.image %}
                        <img src="{{ absolute_url(asset('uploads/' ~ forum.image)) }}">
                    {% else %}
                        <span class="text-muted">Aucune image</span>
                    {% endif %}
                </td>
                <td>{{ forum.datePublication|date('d/m/Y H:i') }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>