{% extends 'base.html.twig' %}

{% block title %}Voyage index{% endblock %}

{% block body %}
    <h1>Voyage index</h1>
    {# Formulaire de recherche #}
<form method="get" action="{{ path('app_voyage_index') }}">
    <input type="text" name="search" value="{{ search }}" placeholder="Rechercher...">
    <button type="submit">Rechercher</button>
</form>

{# Liens de tri #}
<table>
    <thead>
        <tr>
            <th><a href="{{ path('app_voyage_index', {'sort_by': 'emplacement_client', 'order': order == 'asc' ? 'desc' : 'asc'}) }}">Emplacement Client</a></th>
            <th><a href="{{ path('app_voyage_index', {'sort_by': 'date_depart', 'order': order == 'asc' ? 'desc' : 'asc'}) }}">Date de Départ</a></th>
        </tr>
    </thead>
    <tbody>
        {% for voyage in voyages %}
            <tr>
                <td>{{ voyage.emplacement_client }}</td>
                <td>{{ voyage.date_depart|date('d/m/Y H:i') }}</td>
            </tr>
        {% endfor %}
    </tbody>
</table>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date_depart</th>
                <th>Emplacement_client</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for voyage in voyages %}
            <tr>
                <td>{{ voyage.id }}</td>
                <td>{{ voyage.dateDepart ? voyage.dateDepart|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ voyage.emplacementClient }}</td>
                <td>
                    <a href="{{ path('app_voyage_show', {'id': voyage.id}) }}">show</a>
                    <a href="{{ path('app_voyage_edit', {'id': voyage.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_voyage_new') }}">Create new</a>
{% endblock %}
