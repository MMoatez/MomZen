{% extends 'BaseBack.html.twig' %}

{% block title %}New Forum{% endblock %}

{% block body %}
    <div class="d-flex flex-column align-items-center vh-100 justify-content-center">
        <h1 class="mb-4">Create new Forum</h1>

        <div class="w-50">
            {{ include('forum/_form.html.twig') }}
        </div>

        <a href="{{ path('app_forum_index') }}" class="mt-3 text-primary fw-bold">Back to list</a>
    </div>
{% endblock %}


