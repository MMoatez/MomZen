{% extends 'base.html.twig' %}

{% block title %} Modifier une Ambulance {% endblock %}

{% block body %}
<h1>Modifier une Ambulance</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Mettre à jour</button>
{{ form_end(form) }}

<a href="{{ path('app_ambulance_index') }}" class="btn btn-secondary mt-3">Retour</a>
{% endblock %}
